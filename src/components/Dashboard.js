import { h, Component } from 'preact';

class Dashboard extends Component {
    render() {
        return (
            <div>
                Dashboard
            </div>
        );
    }
}

export default Dashboard;