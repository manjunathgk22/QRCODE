import { h, Component } from 'preact';

class Login extends Component {
    render({}, {}) {
        return (
            <div>
                Login
            </div>
        );
    }
}

export default Login;